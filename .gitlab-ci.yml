stages:
  - server17
deploy_server17:
  stage: server17
  only:
    - dev
  tags:
    - server17
  script:
    - docker-compose -p askonnect-$CI_PROJECT_NAME-$CI_COMMIT_BRANCH-test up -d --build
    - docker image prune -f